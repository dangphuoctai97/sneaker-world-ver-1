import { BREAK_POINT } from "./breakpoint ";

export const DEVICE = {
  MOBILE: `(max-width: ${BREAK_POINT.XS})`,
  MOBILE_SM: `(max-width: ${BREAK_POINT.SM})`,
  TABLET: `(max-width: ${BREAK_POINT.LG})`,
  DESKTOP_XL: `(max-width: ${BREAK_POINT.XL})`,
  DESKTOP_XXL: `(max-width: ${BREAK_POINT.XXL})`,
};
